# Development-only dependencies for WboxCLI.
# Mirrors the [project.optional-dependencies] dev section of pyproject.toml.
#
# Install both runtime and dev deps with:
#     pip install -r requirements.txt -r requirements-dev.txt
# Or, preferred:
#     pip install -e ".[dev]"

-r requirements.txt

pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-playwright>=0.4.0
black>=23.12.0
ruff>=0.1.0
mypy>=1.7.0

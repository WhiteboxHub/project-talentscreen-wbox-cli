"""Sync package — Phase 1 (local extraction only, no network calls).

Phase 2 will add a sync client here without requiring any changes to
the extractor, constants, or repository interfaces defined in this package.
"""
